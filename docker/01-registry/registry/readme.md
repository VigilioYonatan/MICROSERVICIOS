# REGISTRY

https://hub.docker.com/_/registry

```bash
 docker run -d -p 5000:5000 --restart always --name registry registry:3
```

<!-- VER CATALOGO DE REPOSITORIOS -->

https://docs.docker.com/reference/api/registry/latest/#tag/overview
